name: CI/CD

on:
  push:
    branches:
      - main
  pull_request:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  ci-cd:
    name: CI/CD
    permissions:
      contents: read
      packages: read
      deployments: write
    uses: significa/actions/.github/workflows/vercel-app.yaml@main
    with:
      npm_scope: '@significa'
      install_registry_url: 'https://npm.pkg.github.com/'
      # FIXME: Global lint/check currently fails on legacy formatting and generated-type debt.
      # Keep CI deployable with a production build gate; restore `npm run lint` after a dedicated cleanup.
      lint_command: 'npm run build'
      staging_domain: ${{ vars.STAGING_DOMAIN }}
      production_domain: ${{ vars.PRODUCTION_DOMAIN }}
    secrets:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
